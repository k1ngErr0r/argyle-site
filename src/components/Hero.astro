---
---

<section id="home" class="hero">
  <div class="hero-content">
    <h1 class="animate-fade-in">
      Managing <strong>your</strong> IT services<br>
      so your business can grow
    </h1>
    <p class="animate-fade-in-delay">
      Argyle Technology can help your business unlock its true potential with our comprehensive services - we can be the perfect solution to your IT and business needs.
    </p>
    <div class="cta-buttons animate-fade-in-delay-2">
      <a href="#services" class="cta-primary">Our Services</a>
      <a href="#contact" class="cta-secondary">Get in Touch</a>
    </div>
  </div>
</section>

<style>
  .hero {
    min-height: calc(100vh - var(--header-height));
    background-image: linear-gradient(rgba(64,63,79,0.86), rgba(64,63,79,0.86)), 
                      url('https://images.unsplash.com/photo-1451187580459-43490279c0fa?q=80&w=2072&auto=format&fit=crop');
    background-size: cover;
    background-position: center;
    background-attachment: fixed;
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    padding: 2rem;
  }

  .hero-content {
    max-width: 800px;
    margin: 0 auto;
  }

  h1 {
    font-size: 3.5rem;
    color: #fff;
    margin-bottom: 1.5rem;
    line-height: 1.2;
  }

  p {
    font-size: 1.2rem;
    color: #fff;
    max-width: 600px;
    margin: 0 auto 2rem;
    opacity: 0.9;
  }

  .cta-buttons {
    display: flex;
    gap: 1rem;
    justify-content: center;
  }

  .cta-primary, .cta-secondary {
    display: inline-block;
    padding: 1rem 2rem;
    border-radius: 4px;
    text-decoration: none;
    font-weight: 500;
    transition: transform 0.2s ease, background-color 0.2s ease;
  }

  .cta-primary {
    background-color: var(--accent);
    color: white;
  }

  .cta-secondary {
    background-color: transparent;
    color: white;
    border: 2px solid white;
  }

  .cta-primary:hover, .cta-secondary:hover {
    transform: translateY(-2px);
  }

  .cta-primary:hover {
    background-color: #e67004;
  }

  .cta-secondary:hover {
    background-color: rgba(255, 255, 255, 0.1);
  }

  .animate-fade-in {
    animation: fadeIn 1s ease-out;
  }

  .animate-fade-in-delay {
    animation: fadeIn 1s ease-out 0.3s backwards;
  }

  .animate-fade-in-delay-2 {
    animation: fadeIn 1s ease-out 0.6s backwards;
  }

  @keyframes fadeIn {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @media (max-width: 768px) {
    h1 {
      font-size: 2.5rem;
    }

    .cta-buttons {
      flex-direction: column;
    }

    .hero {
      background-attachment: scroll;
    }
  }
</style>